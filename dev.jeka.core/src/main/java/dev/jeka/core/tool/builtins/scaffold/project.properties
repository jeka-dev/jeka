##
# This files is supposed to contain options defined at project level, meaning it overrides options defined
# as user level ([USER_HOME]/.jeka/options.properties) but can be overridden by options passed as command line arguments.
#
# Typically, the version of a language or a protocol can be defined here.
##