package dev.jeka.core;
import org.apache.ivy.util.StringUtils;
import dev.jeka.core.api.depmanagement.JkDependencies;
import JkScope;
import JkVersion;
import dev.jeka.core.api.publishing.JkIvyPublication;
import dev.jeka.core.api.publishing.JkMavenPublication;
import JkUtilsIterable;

/**
 * Tututututtutt
 */
public class DepManagedBuild extends Build {

	public static final JkScope DISTRIB = JkScope.of("distrib").descr("Contains Jerkar distribution zip file");