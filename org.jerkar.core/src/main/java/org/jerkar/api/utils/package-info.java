/**
 * Contains Jerkar utility classes (meaning classes holding only static helper methods). These methods are used by
 * Jerkar itself for its automation engine and its build framework but Jerkar users can use it as well if they want to avoid
 * dependencies on component as Jakarta commons or Guava.
 */
package org.jerkar.api.utils;